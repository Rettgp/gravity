# Local dev secrets — copy this file to .env.local and fill in values.
# .env.local is gitignored and never committed.
# In CI/CD, inject these as GitHub Actions secrets instead.

POSTGRES_PASSWORD=
S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
